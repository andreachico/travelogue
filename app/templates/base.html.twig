<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}Skeleton!{% endblock %}</title>
        {% block stylesheets %}
            {# 'app' must match the first argument to addEntry() in webpack.config.js #}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>

    <body>
    {% block header %}
        {% include 'templates/header/header.html.twig' %}
    {% endblock%}

    {% block content %}
        <div class="content">
            <div class="title">{{ title }}</div>
            <div class="nav-bar">
                <ul>
                    <li class="nav-menu"><a href="#">Home</a></li>
                    <li class="nav-menu"><a href="#">About</a></li>
                </ul>
            </div>
        </div>
    {% endblock %}

    {% bloch footer %}
        {% include 'templates/footer/footer.html.twig' %}
    {% endblock %}
    </body>
</html>
